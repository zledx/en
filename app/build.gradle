plugins {
    id 'com.android.application'
}

android {
    namespace 'com.enabcd.navigation'
    compileSdk 35

    defaultConfig {
        applicationId 'com.enabcd.navigation'
        minSdk 23
        targetSdk 35
        versionCode 6
        versionName '1.5.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
